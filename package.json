{
    "name": "bloglist",
    "version": "1.0.0",
    "description": "Bloglist application (frontend + backend)",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run client:dev\" \"npm run server:dev\"",
        "build": "npm run client:build",
        "lint": "npm run client:lint && npm run server:lint",
        "preview": "npm run client:preview",
        "test": "npm run server:test",
        "client:dev": "npm run dev --prefix frontend",
        "client:build": "npm run build --prefix frontend",
        "client:lint": "npm run lint --prefix frontend",
        "client:preview": "npm run preview --prefix frontend",
        "client:test": "npm run test --prefix frontend",
        "server:start": "npm run start --prefix backend",
        "server:dev": "npm run dev --prefix backend",
        "server:test": "npm run test --prefix backend",
        "server:lint": "npm run lint --prefix backend"
    },
    "author": "David Sabalete",
    "license": "MIT",
    "devDependencies": {
        "concurrently": "^9.0.0"
    }
}